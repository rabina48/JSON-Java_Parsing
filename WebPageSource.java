package httpurl;

import java.io.IOException;
import java.io.InputStream;
import java.io.InputStreamReader;
import java.nio.file.Files;
import java.nio.file.Paths;

public class WebPageSource {
    static String url = "https://jsonplaceholder.typicode.com/photos/";
   // InputStreamReader input = new InputStreamReader(InputStream.nullInputStream());
    //String content = new String(Files.readAllBytes(Paths.get(url)));


    public WebPageSource() throws IOException {
    }
}
